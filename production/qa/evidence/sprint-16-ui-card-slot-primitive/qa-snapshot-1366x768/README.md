# QA Snapshot Bundle — `1366 × 768` (Deferred to human operator)

This subdirectory is the canonical destination for the `1366 × 768`
manual playable-client snapshot of the migrated shop panel composed
via the new card-slot primitive (Sprint 16 story 009, Phase 1).

PROMPT 1067 (the `/dev-story` worker for story 009) has no playable-
client runtime available; the snapshot must be captured by a human
operator using the existing `S15-QA-SNAPSHOT-DEFAULT-DEV` flow (PROMPT
1021 / 1023 — `CCGS_QA_SNAPSHOT=1` default-on in dev builds, `F9`
triggers an in-game snapshot).

Drop the snapshot PNG and feedback audit log here. No new infrastructure
is authored by this story.
